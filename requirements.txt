# ==============================================================================
# ISTRUZIONI DI INSTALLAZIONE
# ==============================================================================
# Questo progetto richiede PyTorch con supporto CUDA (GPU) e Coqui TTS.
# A causa delle dimensioni e dei repository specifici, si consiglia l'installazione in 2 STEP:
#
# STEP 1: Installa PyTorch (Versione CUDA 12.1) manualmente:
#    pip install torch==2.5.1+cu121 torchvision==0.20.1+cu121 torchaudio==2.5.1+cu121 --index-url https://download.pytorch.org/whl/cu121
#
# STEP 2: Installa il resto delle dipendenze con questo file:
#    pip install -r requirements.txt
# ==============================================================================

# --- 1. CORE BACKEND (API & LLM) ---
fastapi
uvicorn
pydantic
python-multipart
requests
ollama

# --- 2. STORAGE & UTILS ---
minio
pydub

# --- 3. COMPATIBILITÀ COQUI XTTS (CRITICO) ---
# Queste versioni sono bloccate per evitare conflitti con XTTS v2.
# NON AGGIORNARE NUMPY O TRANSFORMERS!
numpy==1.22.0
transformers==4.33.0
scipy==1.11.4
soundfile==0.13.1

# --- 4. MOTORE TTS ---
# Installazione diretta da GitHub per fix Windows
git+https://github.com/coqui-ai/TTS.git